import type { NextConfig } from "next";

const nextConfig: NextConfig = {
images: {
    domains: ["res.cloudinary.com",'img.freepik.com',
      "t3.ftcdn.net","pin.it","i.pinimg.com"
    ],
  },
  /* config options here */
};

export default nextConfig;
